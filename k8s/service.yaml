apiVersion: v1
kind: Service
metadata:
  name: client-api-service
  namespace: client-portal
spec:
  selector:
    app: client-api
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: NodePort